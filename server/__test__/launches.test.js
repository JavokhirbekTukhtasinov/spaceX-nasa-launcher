describe('test GET / launches', () => { 
    test('it should response with 200 response code', () => { 
        const response = 200;
        expect(response).toBe(200);
    });
    
}
);

describe('test POST / launches', () => {

    test('it should responde with 200 success code ', () => {

    })
    test('it should catch missing required properties ', () => {
     })

    test('it should catch invalid date format ', () => {
    } )
} );